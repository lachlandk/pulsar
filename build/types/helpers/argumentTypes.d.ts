export declare type point2D = {
    x: number;
    y: number;
};
export declare type choice2D = {
    x: boolean;
    y: boolean;
};
export declare type drawingFunction = (context: CanvasRenderingContext2D, timeValue: number) => void;
